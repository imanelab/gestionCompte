{% extends "compteBundle::layout.html.twig" %}

{% block pageContent %}
    <h1>Codificationabb</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ codificationABB.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ codificationABB.code }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ codificationABB.label }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('codificationabb_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('codificationabb_edit', { 'id': codificationABB.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
