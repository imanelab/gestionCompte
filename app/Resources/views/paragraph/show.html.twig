{% extends "compteBundle::layout.html.twig" %}


{% block xTitle %}
    <h2> الفقرات </h2>
{% endblock %}
{% block content %}

 <table id="datatable" class="table table-striped table-bordered">
                      <thead>
                        <tr>
                            
							
							<th>العمليات</th>
                          <th>إسم الفقرة</th>
						  <th>رقم الفقرة</th>
						
                        </tr>
                     </thead>


                      <tbody>
                        <tr>
                         
                          
                         
                       <td>

                          
            <div class="form-group">
                <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                           
							 
							 
							 {{ form_start(delete_form) }}
                           <input type="submit" value="حذف" class="btn btn-danger">
						    <a class="btn btn-info" href="{{ path('paragraph_edit', { 'id': paragraph.id }) }}">تعديل
							
							</a>
                             {{ form_end(delete_form) }}
							 
							
							 
							</div> </div> 
                       </td>
					   <td>{{ paragraph.title }}</td>
					   <td>{{ paragraph.idp }}</td>
					
					   
                        </tr>
            
						</tbody>
                    </table>

    
            <a class="btn btn-primary pull-right" href="{{ path('paragraph_index') }}">العودة إلى اللائحة</a>
       
{% endblock %}

