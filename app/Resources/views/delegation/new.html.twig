{% extends 'compteBundle::layout.html.twig' %}

{% block pageContent %}
    <h1>Delegation creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('delegation_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
