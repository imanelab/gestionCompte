{% extends "compteBundle::layout.html.twig" %}


{% block xTitle %}
    <h2> بيانات الشركاء </h2>
{% endblock %}
{% block content %}

 <table id="datatable" class="table table-striped table-bordered">
                      <thead>
                        <tr>

							
							<th>العمليات</th>
							 <th>نوع الشريك</th>
                          <th>إسم الشريك</th>
						 
						
                        </tr>
                     </thead>


                      <tbody>
                        <tr>
                         
                          
                         
                       <td>

                          
            <div class="form-group">
                <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                           

							 
							 
							 {{ form_start(delete_form) }}
                           
						   <button value="حذف" class="btn btn-danger " > حذف
						   <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
						   </button>
						   
						    <a class="btn btn-info" href="{{ path('externalaccount_edit', { 'id': externalAccount.id }) }}">تعديل
							<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
							</a>
                             {{ form_end(delete_form) }}
							 
							
							 
							</div> </div> 
                       </td>
					   
					   <td>{{ externalAccount.type }}</td>
					  <td>{{ externalAccount.name }}</td>
            
                      
					   
                       
            
						</tbody>
                    </table>

    
            <a class="btn btn-primary pull-right" href="{{ path('externalaccount_index') }}">العودة إلى اللائحة
			<span class="glyphicon glyphicon-share-alt" aria-hidden="true"></span>
			</a>
       
{% endblock %}

