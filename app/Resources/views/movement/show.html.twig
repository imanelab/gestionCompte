{% extends "compteBundle::layout.html.twig" %}


{% block xTitle %}
    <h2> التحويلات </h2>
{% endblock %}
{% block content %}

 <table id="datatable" class="table table-striped table-bordered">
                      <thead>
                        <tr>

                            
                            <th>العمليات</th>
                          <th>تاريخ إنهاء عملية التحويل</th>
                          <th>عدد الأشهر</th>
                          <th>تاريخ التحويل</th>
                          <th>مبلغ التحويل</th>
                        
                        </tr>
                     </thead>


                      <tbody>
                        <tr>
                         
                          
                         
                       <td>

                          
            <div class="form-group">
                <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                           

                             
                             
                             {{ form_start(delete_form) }}
                           <button value="حذف" class="btn btn-danger " > حذف
						   <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
						   </button>
                            <a class="btn btn-info" href="{{ path('movement_edit', { 'id': movement.id }) }}">تعديل
                            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                            </a>




                             <a class="btn btn-info" href="{{ path('movement_request', { 'id': movement.id}) }}">طلب التحويل
                            
                            </a>
                             <a class="btn btn-info" href="{{ path('movement_letter', { 'id': movement.id }) }}">رسالة التحويل
                            
                            </a>
                             {{ form_end(delete_form) }}
                             
                            
                             
                            </div> </div> 
                       </td>
                       
                       <td>{% if movement.realDateMv %}{{ movement.realDateMv|date('Y-m-d H:i:s') }}{% endif %}</td>
                       <td>{{ movement.months }}</td>
                       <td>{% if movement.dateMv %}{{ movement.dateMv|date('Y-m-d H:i:s') }}{% endif %}</td>
                       <td>{{ movement.amountMv }}</td>
                    
                       
                        </tr>
            
                        </tbody>
                    </table>

    
            <a class="btn btn-primary pull-right" href="{{ path('movement_index') }}">العودة إلى اللائحة
			<span class="glyphicon glyphicon-share-alt" aria-hidden="true"></span>
			</a>
       
{% endblock %}
