{% trans_default_domain 'FOSUserBundle' %}


{% block xTitle %}
    <h2> changement de profil</h2>
{% endblock %}
{% block content %}      
      <div class="form-group">
         <label class="control-label col-md-3 col-sm-3 col-xs-12"> {{ 'profile.show.username'|trans }}<span class="required"></span>
         </label>
      <div class="col-md-6 col-sm-6 col-xs-12">
        {{ user.username }}
        </div>
        </div>

        <div class="form-group">
         <label class="control-label col-md-3 col-sm-3 col-xs-12" for="first-name">{{ 'profile.show.email'|trans }}<span class="required"></span>
         </label>
      <div class="col-md-6 col-sm-6 col-xs-12">
        {{ user.email }}
        </div>
        </div>
        
        <div class="ln_solid"></div>
            <div class="form-group">
                <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                <a class="btn btn-primary pull-right" href="{{ path('fos_user_profile_edit') }}">Edit</a>
                <a class="btn btn-primary" href="{{ path('delegation_index') }}">العودة إلى اللائحة</a>
                <a class="btn btn-info" href="{{ path('compte_homepage')}}">Home</a>

            </div>
        </div>
{% endblock %}